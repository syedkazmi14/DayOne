node_modules
dist
scripts/.out
.env
.env.local
.DS_Store
*.log
tsconfig.tsbuildinfo
# generated assets + content database written by server/mediaServer.mjs
# (anchored to the repo root — unanchored "data" also matched src/data/,
# silently hiding new files added there, e.g. src/data/episodeStore.ts)
/storage
/data
# local sample documents for trying Studio ingest
test-docs
